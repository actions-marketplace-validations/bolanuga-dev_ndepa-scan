apiVersion: v1
kind: Pod
metadata:
  name: ndepa-test-violator
  namespace: default
  labels:
    app: ndepa-test
spec:
  containers:
  - name: untrusted-app
    image: alpine:latest
    command: ["sh", "-c", "sleep 3600"]
    securityContext:
      privileged: true
      runAsUser: 0
